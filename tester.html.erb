OG:

<%= form_tag(update_listing_path(list_id: @list.id, movie_id: movie.id), :method => :patch) do %>

<%= form_tag( { :controller => :listings, :action => :update, :method => :patch }, { remote: true } ) do %>

<%= form_tag( { :controller => :listings, :action => :update, :method => :patch, list_id: @list.id, movie_id: movie.id }, { remote: true } ) do %>