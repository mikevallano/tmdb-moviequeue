<h1>Your Rating for <%= @movie.title %></h1>

<h2>You gave <%= @rating.movie.title %> a <%= @rating.value %> out of 10.</h2>

<%= link_to 'Edit Rating', edit_movie_rating_path(@movie, @rating), class: 'btn' %>
<%= link_to "Back to #{@movie.title}", @movie, class: 'btn' %>
