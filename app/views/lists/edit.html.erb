<% content_for(:title, "Editing #{@list.name}") %>

<h1>Editing List</h1>

<%= render 'form' %>

<%= link_to 'Show', user_list_path(@list.owner_id, @list.id) %> |
<%= link_to 'Back', user_lists_path(current_user) %>
