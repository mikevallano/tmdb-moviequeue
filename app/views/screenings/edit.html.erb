<% content_for(:title, "Editing") %>

<h2>Edit your <%= @screening.date_watched.stamp("12-31-2016") %> Screening of <%= link_to @movie.title, @movie %></h2>

<%= render partial: 'form', locals: { movie: @movie, screening: @screening } %>

<%= button_to 'Delete', movie_screening_path(@movie, @screening), method: :delete, data: { confirm: 'Are you sure you want to delete this screening? This action cannot be undone.' } %>
