
  <p><strong>On Lists:</strong>
    <% movie.lists.by_user(current_user).each do |list| %>
      <span class="x"><%= link_to "#{list.name} ", user_list_path(current_user, list.slug), id: "show_list_link_on_list_movies_partial" %></span><%= link_to "", delete_listing_path(list.id, movie.id), id: "remove_movie_link_movies_partial", method: :delete, remote: true, data: { confirm: "Are you sure you want to remove this movie from your " + list.name + " list?" }, class: "fa fa-times-circle" %>
    <% end %> <!-- # list loop -->
  </p>
