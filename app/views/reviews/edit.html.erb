<%= link_to 'Back', :back, class: 'btn inline-block mt-10' %>

<h1>Edit Your Review of <%= link_to @movie.title, @movie %></h1>

<%= render 'form' %>

<%= link_to 'Cancel Changes', movie_review_path(@movie, @review), class: 'btn' %>
