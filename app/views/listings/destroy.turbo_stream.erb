<%= turbo_stream.update(
      "movie_on_lists_#{@movie.tmdb_id}",
      partial: 'movies/lists',
      locals: { movie: @movie }
    )
%>
<!--TODO: handle removing movie from the list when on the current list-->
